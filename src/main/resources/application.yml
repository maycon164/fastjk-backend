spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  mvc:
    validation:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

app:
  client-id: ${GOOGLE_ID}
  secret: ${SECRET}